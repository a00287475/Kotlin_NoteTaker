specVersion: "v1beta"
serviceId: "notetaker"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "notetaker-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
